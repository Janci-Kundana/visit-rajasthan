# Test cases

This table summarizes the feature-level inputs and outcomes from the full
`npm test -- --test-reporter=dot` run on 2026-09-24. `PASS` means the linked
automated cases passed in that run; it does not imply a separate human review.

| Feature             | Input                                                                                                                       | Expected                                                                                                                                      | Actual                                                                                                                                     |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| F1 — Routing        | `""`, `"#"`, `"#/"`, all four `#/{id}` routes, one trailing slash, `#/places/jaipur`, `#/jaipur//`, `#/Jaipur`, `#/jodhpur` | Home and known pages resolve; malformed, nested, or unknown hashes show the places grid, never a blank page.                                  | **PASS — 23/23** in [`routing.test.mjs`](tests/routing.test.mjs).                                                                          |
| F2 — Home scene     | Home enter/leave; hidden tab; reduced motion; missing model or WebGL context                                                | Poster is available immediately; failures keep the still; inactive/reduced-motion states do not run an animation loop.                        | **PASS — 15/15** in [`home-scene.test.mjs`](tests/home-scene.test.mjs).                                                                    |
| F3 — Places grid    | The four entries in `places[]`                                                                                              | One complete card per destination, with correct hash link, poster, accent, and tile hooks.                                                    | **PASS — 6/6** in [`places-grid.test.mjs`](tests/places-grid.test.mjs).                                                                    |
| F4 — Tile cards     | Visible/off-screen cards; hover; model/WebGL failure; reduced motion; route teardown; cold vs warmed model cache | Cards keep their poster fallback; models prefetch on grid mount and warm from home (idle/hover); render work is visibility-gated and bounded; route changes dispose resources. | **PASS — 23/23** in [`tile-cards.test.mjs`](tests/tile-cards.test.mjs). |
| F5 — Place guide    | Each `#/place-id` page plus its map center and bounds                                                                       | The full guide includes a lazy OpenStreetMap viewbox, accurate in-bounds marker, outbound map link, and contributor attribution.              | **PASS — 14/14** in [`place-page.test.mjs`](tests/place-page.test.mjs); map bounds also checked in F7.                                     |
| F6 — About          | `#/about`                                                                                                                   | Colophon content appears without mounting destination tiles or destination-only sections.                                                     | **PASS — 8/8** in [`about.test.mjs`](tests/about.test.mjs).                                                                                |
| F7 — Content data   | Every `Place` entry and each referenced file                                                                                | IDs and content are complete; assets exist; map centers lie inside valid bounds.                                                              | **PASS — 16/16** across [`places.test.mjs`](tests/places.test.mjs) and [`place-content.test.mjs`](tests/place-content.test.mjs).           |
| F8 — Accessibility  | Hidden/shown routes; reduced motion; hidden document; keyboard-facing panels; slow poster loads | Hidden panels stay inert; decorative canvases stay hidden from assistive tech; motion and loading behavior follow the accessibility contract; card/poster colours never flash link-blue. | **PASS — 15/15** in [`ux.test.mjs`](tests/ux.test.mjs). |
| F9 — Asset delivery | Production build under Vite's relative base path                                                                            | Runtime paths remain subpath-safe; shipped asset classes have gzip/Brotli variants.                                                           | **PASS — 7/7** in [`assets.test.mjs`](tests/assets.test.mjs); final build output is recorded in [`day3-results.md`](docs/day3-results.md). |

The feature suites account for **127 passing cases**. The same full run passed
the 22 hook and subagent lifecycle cases: **149 passed, 0 failed, 0 skipped**.

One F9 case checks the built `dist/` output and is skipped when `dist/` does not
exist. Run `npm run build` before `npm test` to get 145/145; on a fresh clone
without a build the result is 144 passed, 1 skipped. Re-verified on 2026-09-24
from a fresh clone: 144 passed and 1 skipped before building; after
`npm run build`, `tests/assets.test.mjs` passed 7/7.
